{% extends '::base_sin_menu.html.twig' %}
{% block title %}Configurar recibo - Soga App{% endblock %}
{% block content %}
    <section class="content-header">
        <h1>
            Cartera
            <small>Recibo</small>
        </h1>
    </section>    
    <section class="content">        	
        {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}
        <div class="panel panel-default">
            <div class="panel-heading">Imputacion contable</div>
            <div class="panel-body">
                <div>                      
                    <table class="table table-striped table-bordered table-condensed table-hover">
                        <tr>
                            <th>CÓDIGO</th>
                            <th>NOMBRE</th>
                            <th>CUENTA</th>
                        </tr>
                        {% for arConfiguracion in arConfiguracion %}
                        {% endfor %}
                    </table>                                
                    <div class="form-group form-group-sm">
                        <label class="col-sm-4 control-label">Codigo comprobante:</label>
                        <div class="col-sm-2">
                            {{ form_widget(form.TxtCodigoComprobante) }}
                        </div>
                    </div>                    
                </div>                      
            </div>
        </div>                   
        <div class="modal-footer">        
            {{ form_widget(form.BtnGuardar, { 'attr': {'class': 'btn btn-sm btn-primary'} }) }}            
        </div>        
        {{ form_end(form) }}
        <br/><br/><br/><br/>
    </section>
{% endblock %}