{% extends '::base_sin_menu.html.twig' %}
{% block title %}Cobro detalle examen nuevo - Soga App{% endblock %}
{% block content %}
    <section class="content-header">
        <h1>
            Recurso humano
            <small>Cobro: detalle examen nuevo</small>
        </h1>
    </section>    
    <section class="content">        	        
        {{ form_start(form, { 'attr': {'class': 'form-horizontal'} }) }}            
        <table class="table table-striped table-bordered table-condensed">
            <tr>
                <th>ID</th>
                <th>NOMBRE</th>                                
                <th style="text-align: right">VALOR</th>
                <th><input type="checkbox" name="ChkSeleccionarTodos" OnClick="ChequearTodosTabla(this, 'ChkSeleccionar[]');"/></th>
            </tr>
            {% for arExamenes in arExamenes %}            
                <tr> 
                    <td><input type="hidden" name="LblCodigo[]" value="{{ arExamenes.codigoExamenPk }}">{{ arExamenes.codigoExamenPk }}</td>                  
                    <td>{{ arExamenes.nombreCorto }}</td>               
                    <td style="text-align: right">{{ arExamenes.vrTotal | number_format(0, '.', ',') }}</td>
                    <td style="text-align: center"><input type="checkbox" name="ChkSeleccionar[]" value="{{ arExamenes.codigoExamenPk }}" /></td>
                </tr>
            {% endfor %}                                
        </table>
        {{ knp_pagination_render(arExamenes) }}
        <div class="btn-toolbar" style="float: left">
            <!-- Botones Menu Items -->
            <div class="btn-group">
                <div class="btn btn-default btn-sm">Registros <span class="badge">{{ arExamenes.getTotalItemCount }}</span></div>                        
            </div>
        </div> 
        <div class="btn-toolbar" style="float: right">
            <!-- Botones Menu Items -->
            <div class="btn-group">
                {{ form_widget(form.BtnAgregar, { 'attr': {'class': 'btn btn-primary btn-sm'} }) }}
            </div>
        </div>            

        {{ form_end(form) }}
        <br/><br/><br/><br/>
    </section>
{% endblock %}