<nav class="navbar navbar-static-top">
    <!-- Sidebar toggle button-->
    <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
    </a>

    <div class="navbar-custom-menu">
        <ul class="nav navbar-nav">
            <!--
            <li class="dropdown user user-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">                    
                    <span class="hidden-xs">Advertencia de suspension servicio</span>
                </a>
            </li>            
            -->
            <!-- Messages: style can be found in dropdown.less-->
            <li class="dropdown messages-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="fa fa-envelope-o"></i>
                    <span class="label label-success">1</span>
                </a>
                <ul class="dropdown-menu">
                    <li class="header">Tienes 1 mensaje</li>
                    <li>
                        <!-- inner menu: contains the actual data -->
                        <ul class="menu">
                            <li><!-- start message -->
                                <a href="#">
                                    <div class="pull-left">
                                        <img src="{{ asset('recursos/dist/img/user10-160x160.png') }}" class="img-circle" alt="User Image">
                                    </div>
                                    <h4>
                                        Equipo de soporte
                                        <small><i class="fa fa-clock-o"></i> 12 horas</small>
                                    </h4>
                                    <p>Esta a gusto con la nueva interface?</p>
                                </a>
                            </li>
                            <!-- end message -->
                        </ul>
                    </li>
                    <li class="footer"><a href="#">Ver todos los mensajes</a></li>
                </ul>
            </li>
            <!-- Notifications: style can be found in dropdown.less -->
            <li class="dropdown notifications-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="fa fa-bell-o"></i>
                    {% if (app.user.notificacionesPendientes) %}
                        <span class="label label-danger">{{ app.user.notificacionesPendientes }}</span>
                    {% endif %}
                </a>
                <ul class="dropdown-menu">
                    <li class="header">Tienes {{ app.user.notificacionesPendientes| default(0) }} notificaciones</li>
                    <li>
                        <!-- inner menu: contains the actual data -->
                        <ul class="menu">

                        </ul>
                    </li>
                    <li class="footer"><a href="#">Ver todo</a></li>
                </ul>
            </li>
            <!-- Tasks: style can be found in dropdown.less -->
            <li class="dropdown tasks-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <i class="fa fa-flag-o"></i>
                    {% if (app.user.tareasPendientes) %}
                    <span class="label label-warning">{{ app.user.tareasPendientes }}</span>
                    {% endif %}
                </a>
                <ul class="dropdown-menu">
                    <li class="header">Tienes {{ app.user.tareasPendientes| default(0) }} tareas</li>
                    <li>
                        <!-- inner menu: contains the actual data -->
                        <ul class="menu">

                        </ul>
                    </li>
                    <li class="footer">
                        <a href="#">Ver todas las tareas</a>
                    </li>
                </ul>
            </li>
            <!-- User Account: style can be found in dropdown.less -->
            <li class="dropdown user user-menu">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <img src="{{ asset('recursos/dist/img/user10-160x160.png') }}" class="user-image" alt="User Image">
                    <span class="hidden-xs">{{ app.user.nombreCorto|title }}</span>
                </a>
                <ul class="dropdown-menu">
                    <!-- User image -->
                    <li class="user-header">
                        <img src="{{ asset('recursos/dist/img/user10-160x160.png') }}" class="img-circle" alt="User Image">

                        <p>
                            {{ app.user.nombreCorto|title }} - Usuario
                            <small>Ultimo ingreso Ene. 2017</small>
                        </p>
                    </li>
                    <!-- Menu Body -->
                    <li class="user-body">
                        <div class="row">
                            <div class="col-xs-4 text-center">
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a href="{{ path('brs_seg_admin_usuario_lista') }}">Usuarios</a>
                                {% else %}                                
                                    <a href="#">Usuarios</a>
                                {% endif %}
                            </div>
                            <div class="col-xs-4 text-center">
                                <a href="#">Configuracion</a>
                            </div>
                            <div class="col-xs-4 text-center">
                                <a href="#">Datos</a>
                            </div>
                        </div>
                        <!-- /.row -->
                    </li>
                    <!-- Menu Footer-->
                    <li class="user-footer">
                        <div class="col-xs-4 text-left">
                            <a href="{{ path('brs_seg_usuario_detalle_ver', { 'codigoUsuario': app.user.id }) }}">Perfil</a>
                        </div>
                        <div class="pull-right">
                            <a href="{{ path('logout') }}" class="btn btn-default btn-flat">Cerrar sesion</a>
                        </div>
                    </li>
                </ul>
            </li>
            <!-- Control Sidebar Toggle Button -->
            <li>
                <a href="#" data-toggle="control-sidebar"><i class="fa fa-gears"></i></a>
            </li>
        </ul>
    </div>
</nav>